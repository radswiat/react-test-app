import apiCfg from './_api';

/**
 * Config file SHOULD NOT export any configuration directly,
 * please split your configuration files between separate files,
 * then import them here, and export as shown below
 */
export {
  apiCfg,
};
